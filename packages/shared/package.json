{
  "name": "@shared/types",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"ok\"",
    "test": "jest --selectProjects shared",
    "test:watch": "jest --selectProjects shared --watch",
    "test:cov": "jest --selectProjects shared --coverage"
  },
  "dependencies": {
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
